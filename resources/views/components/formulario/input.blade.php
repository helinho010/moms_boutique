<div>
    <div class="mb-3">
        <input type="{{ $tipo }}"
               name="{{ $name }}" 
               class="form-control" 
               id="{{ $id }}" 
               placeholder="{{ $placeholder }}">
    </div>
</div>